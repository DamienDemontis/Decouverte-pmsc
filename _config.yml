# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Découverte des Spécialités MSc Epitech
email: equipe-pedagogique@epitech.eu
description: >- # this means to ignore newlines until "baseurl:"
  Ce site présente les différentes spécialités disponibles dans le cursus MSc d'Epitech,
  avec des projets de découverte et des ressources pour vous aider à choisir votre orientation.
baseurl: "" # the subpath of your site, e.g. /blog - Empty for production deployment
url: "http://localhost:4000" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: Epitech
github_username: Epitech

# Build settings
theme: minima
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap

# SEO settings
author: Damien Demontis
social:
  name: Epitech MSc
  links:
    - https://github.com/Epitech
    - https://twitter.com/Epitech

# Performance settings
incremental: true
profile: false
safe: true

# Markdown settings
markdown: kramdown
kramdown:
  input: GFM
  hard_wrap: false
  syntax_highlighter: rouge

# Sass configuration
sass:
  style: compressed
  sourcemap: development

# Collections pour les spécialités
collections:
  specialites:
    output: true
    permalink: /:collection/:name

# Configuration par défaut pour les pages de spécialités
defaults:
  - scope:
      path: ""
      type: "specialites"
    values:
      layout: "specialite"

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
